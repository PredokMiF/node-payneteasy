"use strict";

/* Описание работы с числами.
 * В валидатор сразу можно передавать элементарные типы, в частности числа:
 */

var v = require('../app');

module.exports = {
    // Проверяем что валидация по лог. работает
    'test-bool-c4dfd31c-af7f-4a0a-a3ce-cf9c61eedbb2': function (test) {
        var tpl = v('boolean');
        tpl(true).
            then(function (result) {
                test.equals(result, true);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-68d1d9df-ba85-4794-81b7-89c9164c68b2': function (test) {
        var tpl = v('boolean');
        tpl(false).
            then(function (result) {
                test.equals(result, false);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-b648a813-4268-4495-b011-1493799fdaba': function (test) {
        var tpl = v('bool');
        tpl(true).
            then(function (result) {
                test.equals(result, true);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-11946d15-7889-4eb4-a005-9c7594063735': function (test) {
        var tpl = v('bool');
        tpl(false).
            then(function (result) {
                test.equals(result, false);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-ca4ac5f6-c3ad-4948-83e9-a4bba462b6d2': function (test) {
        var tpl = v('b');
        tpl(true).
            then(function (result) {
                test.equals(result, true);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-84a9b8ef-deb1-4c1b-a8ac-42d7c3130664': function (test) {
        var tpl = v('b');
        tpl(false).
            then(function (result) {
                test.equals(result, false);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    // Ломаем
    'test-bool-fa6b29d5-3c6a-461a-b8b0-211d10978c2d': function (test) {
        var tpl = v('b');
        tpl().
            then(function (result) {
                test.ok(false, 'Должна быть ошибка');
            }, function (err) {
                test.ok(err);
            }).done(function () { test.done(); });
    },
    'test-bool-9f3eea7e-d80d-4f98-aa2d-2836fdfa3fdf': function (test) {
        var tpl = v('b');
        tpl(undefined).
            then(function (result) {
                test.ok(false, 'Должна быть ошибка');
            }, function (err) {
                test.ok(err);
            }).done(function () { test.done(); });
    },
    'test-bool-8c08fb78-e27b-431c-9fb6-66296442a734': function (test) {
        var tpl = v('b');
        tpl(null).
            then(function (result) {
                test.ok(false, 'Должна быть ошибка');
            }, function (err) {
                test.ok(err);
            }).done(function () { test.done(); });
    },
    'test-bool-d0dfacb1-e1b3-4d0d-978d-007c114c33ee': function (test) {
        var tpl = v('b');
        tpl(0).
            then(function (result) {
                test.ok(false, 'Должна быть ошибка');
            }, function (err) {
                test.ok(err);
            }).done(function () { test.done(); });
    },
    'test-bool-98eb9d1b-f668-4e3a-888c-2511a4abd79b': function (test) {
        var tpl = v('b');
        tpl(1).
            then(function (result) {
                test.ok(false, 'Должна быть ошибка');
            }, function (err) {
                test.ok(err);
            }).done(function () { test.done(); });
    },
    'test-bool-be27151e-0d62-447f-8744-f6c3111b5041': function (test) {
        var tpl = v('b');
        tpl('').
            then(function (result) {
                test.ok(false, 'Должна быть ошибка');
            }, function (err) {
                test.ok(err);
            }).done(function () { test.done(); });
    },
    'test-bool-d2eda771-7e5d-49dd-a767-51ac3cab6705': function (test) {
        var tpl = v('b');
        tpl('0').
            then(function (result) {
                test.ok(false, 'Должна быть ошибка');
            }, function (err) {
                test.ok(err);
            }).done(function () { test.done(); });
    },
    'test-bool-10cda441-fce8-4930-81d8-b08dbe2584e7': function (test) {
        var tpl = v('b');
        tpl('1').
            then(function (result) {
                test.ok(false, 'Должна быть ошибка');
            }, function (err) {
                test.ok(err);
            }).done(function () { test.done(); });
    },
    'test-bool-1a605ba2-1fd9-49f6-bf78-3042aa64a3db': function (test) {
        var tpl = v('b');
        tpl([]).
            then(function (result) {
                test.ok(false, 'Должна быть ошибка');
            }, function (err) {
                test.ok(err);
            }).done(function () { test.done(); });
    },
    'test-bool-5bd1655b-ce1b-44a4-906e-b2900da35a61': function (test) {
        var tpl = v('b');
        tpl({}).
            then(function (result) {
                test.ok(false, 'Должна быть ошибка');
            }, function (err) {
                test.ok(err);
            }).done(function () { test.done(); });
    },
    'test-bool-b3407cb1-6591-4e46-b73a-2fc491292b92': function (test) {
        var tpl = v('b');
        tpl(function(){}).
            then(function (result) {
                test.ok(false, 'Должна быть ошибка');
            }, function (err) {
                test.ok(err);
            }).done(function () { test.done(); });
    },
    // Конверт из числа
    'test-bool-5628ad83-8732-47dc-9dbb-c502f2a90279': function (test) {
        var tpl = v('b convert number');
        tpl(1).
            then(function (result) {
                test.equals(result, true);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-49bd925b-4fda-4475-8838-c0b9c7dcaa1f': function (test) {
        var tpl = v('b convert number');
        tpl(0).
            then(function (result) {
                test.equals(result, false);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-a4d07d85-190f-4ead-81d9-33c8f99e790f': function (test) {
        var tpl = v('b convert number');
        tpl(100).
            then(function (result) {
                test.equals(result, true);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-fc67d79f-ecb8-4dea-95dd-daf9f7e4e60a': function (test) {
        var tpl = v('b convert number');
        tpl(0.0001).
            then(function (result) {
                test.equals(result, true);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-edcc0975-8400-40aa-bee3-c1c19db98234': function (test) {
        var tpl = v('b convert number');
        tpl(-1).
            then(function (result) {
                test.equals(result, true);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    // Ломаем
    'test-bool-f9cbe7ea-42a3-4280-a8f8-8a578512be53': function (test) {
        var tpl = v('b convert number');
        tpl().
            then(function (result) {
                test.ok(false, 'Должна быть ошибка');
            }, function (err) {
                test.ok(err);
            }).done(function () { test.done(); });
    },
    'test-bool-fe7ecde9-590a-4feb-bc88-2f8987393b0b': function (test) {
        var tpl = v('b convert number');
        tpl(null).
            then(function (result) {
                test.ok(false, 'Должна быть ошибка');
            }, function (err) {
                test.ok(err);
            }).done(function () { test.done(); });
    },
    'test-bool-bc0a986a-ec9f-45d7-819f-d8b17849a3a9': function (test) {
        var tpl = v('b convert number');
        tpl(undefined).
            then(function (result) {
                test.ok(false, 'Должна быть ошибка');
            }, function (err) {
                test.ok(err);
            }).done(function () { test.done(); });
    },
    'test-bool-ffcc01d3-f86a-4a65-92f0-a2023c3be775': function (test) {
        var tpl = v('b convert number');
        tpl('').
            then(function (result) {
                test.ok(false, 'Должна быть ошибка');
            }, function (err) {
                test.ok(err);
            }).done(function () { test.done(); });
    },
    'test-bool-8d406b10-e2d5-4b98-a560-fb143c747c9f': function (test) {
        var tpl = v('b convert number');
        tpl('0').
            then(function (result) {
                test.ok(false, 'Должна быть ошибка');
            }, function (err) {
                test.ok(err);
            }).done(function () { test.done(); });
    },
    // Конверт из строки
    'test-bool-7186225f-d897-4e3b-b391-d5d8593fcd6a': function (test) {
        var tpl = v('b convert sNumber');
        tpl('0').
            then(function (result) {
                test.equals(result, false);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-ff88ae40-f764-4e64-927c-287c337a87d4': function (test) {
        var tpl = v('b convert sNumber');
        tpl('1').
            then(function (result) {
                test.equals(result, true);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-125a95d1-fd9e-4f42-b3d2-eaf06078cb16': function (test) {
        var tpl = v('b convert sNum');
        tpl('0').
            then(function (result) {
                test.equals(result, false);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-c2bf3059-263b-463c-9081-6f6ef26b239b': function (test) {
        var tpl = v('b convert sNum');
        tpl('1').
            then(function (result) {
                test.equals(result, true);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-17324ee3-5118-48a3-add6-f97bbb8e8ea6': function (test) {
        var tpl = v('b convert sNumber');
        tpl('100').
            then(function (result) {
                test.equals(result, true);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-7f31fbec-9dd0-4738-9c53-14de4617d482': function (test) {
        var tpl = v('b convert sNumber');
        tpl('-1').
            then(function (result) {
                test.equals(result, true);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-d290d79f-6b74-4f80-93af-532481b9d112': function (test) {
        var tpl = v('b convert sNumber');
        tpl('0.00001').
            then(function (result) {
                test.equals(result, true);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    // Ломаем
    'test-bool-25d56495-4614-44df-9ea7-7abf08f9771e': function (test) {
        var tpl = v('b convert sNumber');
        tpl('true').
            then(function (result) {
                test.ok(false, 'Должна быть ошибка');
            }, function (err) {
                test.ok(err);
            }).done(function () { test.done(); });
    },
    'test-bool-ada20e9b-0ac8-4d00-baba-701127661a05': function (test) {
        var tpl = v('b convert sNumber');
        tpl('on').
            then(function (result) {
                test.ok(false, 'Должна быть ошибка');
            }, function (err) {
                test.ok(err);
            }).done(function () { test.done(); });
    },
    'test-bool-cdd18228-6be6-4399-9ab7-7e87a4ca5e61': function (test) {
        var tpl = v('b convert sNumber');
        tpl('qe').
            then(function (result) {
                test.ok(false, 'Должна быть ошибка');
            }, function (err) {
                test.ok(err);
            }).done(function () { test.done(); });
    },
    'test-bool-e347c268-1ae8-4bcb-8aeb-77d55ad36b22': function (test) {
        var tpl = v('b convert sNumber');
        tpl('0q').
            then(function (result) {
                test.ok(false, 'Должна быть ошибка');
            }, function (err) {
                test.ok(err);
            }).done(function () { test.done(); });
    },
    'test-bool-5c6cb723-c935-41d7-adb1-21cd40f65e09': function (test) {
        var tpl = v('b convert sNumber');
        tpl(0).
            then(function (result) {
                test.ok(false, 'Должна быть ошибка');
            }, function (err) {
                test.ok(err);
            }).done(function () { test.done(); });
    },
    // Конверт из строк on и off
    'test-bool-438fb664-f79e-4f15-a7f4-971b5f54c303': function (test) {
        var tpl = v('b convert on');
        tpl('on').
            then(function (result) {
                test.equals(result, true);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-4655d501-b38e-4e78-8a27-1f11b586f4e1': function (test) {
        var tpl = v('b convert on');
        tpl('off').
            then(function (result) {
                test.equals(result, false);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-8ecb43a8-595b-4ce8-9897-abb561508326': function (test) {
        var tpl = v('b convert off');
        tpl('on').
            then(function (result) {
                test.equals(result, true);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-bf78a977-b100-4ad4-b373-bb8f3b3220fb': function (test) {
        var tpl = v('b convert off');
        tpl('off').
            then(function (result) {
                test.equals(result, false);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    // Ломаем
    'test-bool-b5898438-7dfd-409c-8a71-955ec2b471c0': function (test) {
        var tpl = v('b convert on');
        tpl(0).
            then(function (result) {
                test.ok(false, 'Должна быть ошибка');
            }, function (err) {
                test.ok(err);
            }).done(function () { test.done(); });
    },
    'test-bool-23d7f172-d255-4f7e-8f2e-615d10b685d4': function (test) {
        var tpl = v('b convert on');
        tpl('0').
            then(function (result) {
                test.ok(false, 'Должна быть ошибка');
            }, function (err) {
                test.ok(err);
            }).done(function () { test.done(); });
    },
    'test-bool-c575585c-d912-45dc-a790-188b4e286be1': function (test) {
        var tpl = v('b convert on');
        tpl('true').
            then(function (result) {
                test.ok(false, 'Должна быть ошибка');
            }, function (err) {
                test.ok(err);
            }).done(function () { test.done(); });
    },
    // Конверт из строк true и false
    'test-bool-6502edbc-23a3-4053-add8-52d1594a21ad': function (test) {
        var tpl = v('b convert true');
        tpl('true').
            then(function (result) {
                test.equals(result, true);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-83d1a5f0-bdaf-49f1-928d-f834c3c879bd': function (test) {
        var tpl = v('b convert true');
        tpl('false').
            then(function (result) {
                test.equals(result, false);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-1af0f27d-fcd4-4a32-8a35-730808653b7e': function (test) {
        var tpl = v('b convert false');
        tpl('true').
            then(function (result) {
                test.equals(result, true);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-a0290219-1a11-4760-ace1-8bc665291532': function (test) {
        var tpl = v('b convert false');
        tpl('false').
            then(function (result) {
                test.equals(result, false);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    // Ломаем
    'test-bool-219d86a1-f9b2-46ea-b56e-15585e4bbce2': function (test) {
        var tpl = v('b convert true');
        tpl(0).
            then(function (result) {
                test.ok(false, 'Должна быть ошибка');
            }, function (err) {
                test.ok(err);
            }).done(function () { test.done(); });
    },
    'test-bool-c8aaebcc-6f77-4835-9f15-d22123d6df9d': function (test) {
        var tpl = v('b convert true');
        tpl('0').
            then(function (result) {
                test.ok(false, 'Должна быть ошибка');
            }, function (err) {
                test.ok(err);
            }).done(function () { test.done(); });
    },
    'test-bool-25f6c1fc-83b4-4862-a290-9142de4a30b5': function (test) {
        var tpl = v('b convert true');
        tpl('on').
            then(function (result) {
                test.ok(false, 'Должна быть ошибка');
            }, function (err) {
                test.ok(err);
            }).done(function () { test.done(); });
    },
    // Конверт из строк и чисел
    'test-bool-83a11fff-9dc2-4a48-9124-c7d6bf0f591b': function (test) {
        var tpl = v('b convert num,sNum,on,true');
        tpl(1).
            then(function (result) {
                test.equals(result, true);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-42bd12b8-3473-4d00-be72-91f5ddd24d76': function (test) {
        var tpl = v('b convert num,sNum,on,true');
        tpl('false').
            then(function (result) {
                test.equals(result, false);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-0464f664-dbb0-472d-9fd4-01d0ee3c6a5c': function (test) {
        var tpl = v('b convert num,sNum,on,true');
        tpl(0).
            then(function (result) {
                test.equals(result, false);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-d65a21a2-e51e-4cce-8499-5bfac223e762': function (test) {
        var tpl = v('b convert number,true');
        tpl(1).
            then(function (result) {
                test.equals(result, true);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    // Ломаем
    'test-bool-77f316a5-0d29-46eb-ab2a-c45afa1ed6d1': function (test) {
        var tpl = v('b convert num,on,true');
        tpl('0').
            then(function (result) {
                test.ok(false, 'Должна быть ошибка');
            }, function (err) {
                test.ok(err);
            }).done(function () { test.done(); });
    },
    'test-bool-695bc7f4-6807-470d-9b09-db8fc5d37543': function (test) {
        var tpl = v('b convert num,sNum,true');
        tpl('on').
            then(function (result) {
                test.ok(false, 'Должна быть ошибка');
            }, function (err) {
                test.ok(err);
            }).done(function () { test.done(); });
    },
    // Конверт, все вместе
    'test-bool-aeba0997-2a56-4d2c-b2fa-43a3278d9074': function (test) {
        var tpl = v('b convert num,sNum,on,true');
        tpl(1).
            then(function (result) {
                test.equals(result, true);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-581cfcaa-cea7-4469-93af-e629fbd2b6d5': function (test) {
        var tpl = v('b convert num,sNum,on,true');
        tpl(0).
            then(function (result) {
                test.equals(result, false);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-d243205a-7889-4e67-80d3-0407513025c1': function (test) {
        var tpl = v('b convert num,sNum,on,true');
        tpl('true').
            then(function (result) {
                test.equals(result, true);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-d68e4584-b45a-44bb-a2ad-883faa96427f': function (test) {
        var tpl = v('b convert num,sNum,on,true');
        tpl('false').
            then(function (result) {
                test.equals(result, false);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-c890a390-b2e0-42d3-80db-8ce07037211a': function (test) {
        var tpl = v('b convert num,sNum,on,true');
        tpl('1').
            then(function (result) {
                test.equals(result, true);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-3e6a4461-d440-49bd-b47c-bd49494ea609': function (test) {
        var tpl = v('b convert num,sNum,on,true');
        tpl('0').
            then(function (result) {
                test.equals(result, false);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-747da64f-4876-4385-af0f-fcd1caa6b185': function (test) {
        var tpl = v('b convert num,sNum,on,true');
        tpl('on').
            then(function (result) {
                test.equals(result, true);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-298b30e2-bb65-4f8a-8569-3a3a8f3b91f0': function (test) {
        var tpl = v('b convert num,sNum,on,true');
        tpl('off').
            then(function (result) {
                test.equals(result, false);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    // Ломаем
    'test-bool-8fdba234-1a44-40a1-ab9b-23edba218dd0': function (test) {
        var tpl = v('b convert num,sNum,on,true');
        tpl('').
            then(function (result) {
                test.ok(false, 'Должна быть ошибка');
            }, function (err) {
                test.ok(err);
            }).done(function () { test.done(); });
    },
    'test-bool-d0cfb87b-be20-47f1-bf1e-3dd5f8b9900b': function (test) {
        var tpl = v('b convert num,sNum,on,true');
        tpl('er').
            then(function (result) {
                test.ok(false, 'Должна быть ошибка');
            }, function (err) {
                test.ok(err);
            }).done(function () { test.done(); });
    },
    // Конверт, все вместе
    'test-bool-e65e827e-65ff-4c89-a6be-07d3d9b8250c': function (test) {
        var tpl = v('b convert all');
        tpl(1).
            then(function (result) {
                test.equals(result, true);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-c14de6c4-d377-475b-8a1d-4e2120c46b80': function (test) {
        var tpl = v('b convert all');
        tpl(0).
            then(function (result) {
                test.equals(result, false);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-e1cdec92-6a01-41e2-8a35-ad077ecaced9': function (test) {
        var tpl = v('b convert all');
        tpl('true').
            then(function (result) {
                test.equals(result, true);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-cee752e0-8b30-4f8e-bacc-d5e561b6e3ea': function (test) {
        var tpl = v('b convert all');
        tpl('false').
            then(function (result) {
                test.equals(result, false);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-33564b80-7ddc-40d0-9b4f-8eb0c5b5452c': function (test) {
        var tpl = v('b convert all');
        tpl('1').
            then(function (result) {
                test.equals(result, true);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-2bb9162b-a3c4-40c1-bc50-cc238a74ec1f': function (test) {
        var tpl = v('b convert all');
        tpl('0').
            then(function (result) {
                test.equals(result, false);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-d2fa8fd6-ab26-4342-9941-841133be0331': function (test) {
        var tpl = v('b convert all');
        tpl('on').
            then(function (result) {
                test.equals(result, true);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-14c43180-3cda-4057-80fc-c7a61275a66b': function (test) {
        var tpl = v('b convert all');
        tpl('off').
            then(function (result) {
                test.equals(result, false);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    // Ломаем
    'test-bool-abbaabdb-d54f-470d-baf0-d651aff63e7b': function (test) {
        var tpl = v('b convert all');
        tpl('').
            then(function (result) {
                test.ok(false, 'Должна быть ошибка');
            }, function (err) {
                test.ok(err);
            }).done(function () { test.done(); });
    },
    'test-bool-f3460481-2855-43fb-a1d8-f3f4fc5fdb48': function (test) {
        var tpl = v('b convert all');
        tpl('er').
            then(function (result) {
                test.ok(false, 'Должна быть ошибка');
            }, function (err) {
                test.ok(err);
            }).done(function () { test.done(); });
    },
    'test-bool-c5a04f5e-2bb3-4505-9e65-5c6a8b78206c': function (test) {
        var tpl = v(['b convert all', function (obj, tpl, cfg) { return obj === true; }]);
        tpl('true').
            then(function (result) {
                test.equals(result, true);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-408ddf4f-d26a-4a15-994a-1dfef6e2d1fa': function (test) {
        var tpl = v(['b convert all', function (obj, tpl, cfg) { return obj === false; }]);
        tpl('off').
            then(function (result) {
                test.equals(result, false);
            }, function (err) {
                test.ok(false, 'Ошибки быть не должно: ' + err);
            }).done(function () { test.done(); });
    },
    'test-bool-4811b642-a8ca-44f7-9d77-bcb2b1447aab': function (test) {
        var tpl = v(['b convert all', 1]);
        tpl(true).
            then(function (result) {
                test.ok(false, 'Должна быть ошибка');
            }, function (err) {
                test.ok(err);
            }).done(function () { test.done(); });
    },
    'test-bool-86de61ac-1f61-4dd1-8691-97a94680d212': function (test) {
        var tpl = v(['b convert all', function(obj, tpl, cfg) { return !(obj === true); }]);
        tpl(true).
            then(function (result) {
                test.ok(false, 'Должна быть ошибка');
            }, function (err) {
                test.ok(err);
            }).done(function () { test.done(); });
    },
    'test-bool-ed5dc804-4800-4815-90d8-a401dd2f0e18': function (test) {
        var tpl = v(['b convert all', function(obj, tpl, cfg) { return !(obj === false); }]);
        tpl(false).
            then(function (result) {
                test.ok(false, 'Должна быть ошибка');
            }, function (err) {
                test.ok(err);
            }).done(function () { test.done(); });
    },
};
