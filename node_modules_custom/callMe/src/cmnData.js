"use strict";

var path = require('path');

var SEC = 1000;
var MINUTE = 60 * SEC;

module.exports = {
    // db
    DB_UPDATER_SRC: path.join(__dirname, '..', 'dao/dbUpdater'),
    EVENT_POOL_INTERVAL: 10 * MINUTE,
    EVENT_DEADBLOCK_FIND_POOL_INTERVAL: 10 * MINUTE,

    eventCallbacks: {},

    callMe: {},
    callMeTypes: {}
};